{
  "name": "accevum",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "npm run -w apps/api build && npm run -w apps/worker build && npm run -w apps/web build",
    "start:api": "node apps/api/dist/server.js",
    "start:worker": "node apps/worker/dist/index.js",
    "dev": "concurrently \"npm run -w apps/api dev\" \"npm run -w apps/worker dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
